info:
  name: Supprimer une tache invalide
  type: http
  seq: 23

http:
  method: DELETE
  url: "{{baseURL}}{{apiTasks}}/:id"
  params:
    - name: id
      value: b9c792a3-2c6a-43b-bc85-f92ba323c22c
      type: path
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("le statut est en erreur (4xx)", function () {
          expect(res.getStatus()).to.be.within(400, 499);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
