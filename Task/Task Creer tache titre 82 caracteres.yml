info:
  name: Task Creer tache titre 82 caracteres
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseURL}}{{apiTasks}}"
  body:
    type: json
    data: |-
      {
        "title": "Ceci est un titre. Ceci est un titre. Ceci est un titre. Ceci est un titre. Ceci e",
        "description": "ici description"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("le statut est 422", function () {
          expect(res.getStatus()).to.equal(422);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
