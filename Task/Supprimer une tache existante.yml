info:
  name: Supprimer une tache existante
  type: http
  seq: 22

http:
  method: DELETE
  url: "{{baseURL}}{{apiTasks}}/{{taskId}}"
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("le statut est 204", function () {
          expect(res.getStatus()).to.equal(204);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
