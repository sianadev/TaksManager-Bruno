info:
  name: Recuperer une tache via Id invalide
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseURL}}{{apiTasks}}/:id"
  params:
    - name: id
      value: testid
      type: path

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
