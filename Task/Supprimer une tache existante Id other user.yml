info:
  name: Supprimer une tache existante Id other user
  type: http
  seq: 24

http:
  method: DELETE
  url: "{{baseURL}}{{apiTasks}}/:id"
  params:
    - name: id
      value: ccce9e75-91ea-4823-9735-2d61025d857f
      type: path
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("le statut est 422", function () {
          expect(res.getStatus()).to.equal(422);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
