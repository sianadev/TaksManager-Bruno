info:
  name: Task Creer tache description 451 caracteres
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseURL}}{{apiTasks}}"
  body:
    type: json
    data: |-
      {
        "title": "Un titre de test",
        "description": "ici description. ici description. ici description. ici description. ici description. ici description. ici description. ici description. ici description. ici description. ici description. ici description. ici description. ici description. ici description. ici description. ici description. ici description. ici description. ici description. ici description. ici description. ici description. ici description. ici description. ici description. ici descr"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("le statut est 422", function () {
          expect(res.getStatus()).to.equal(422);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
