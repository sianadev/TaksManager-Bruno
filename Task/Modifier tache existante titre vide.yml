info:
  name: Modifier tache existante titre vide
  type: http
  seq: 15

http:
  method: PATCH
  url: "{{baseURL}}{{apiTasks}}/{{taskId}}"
  body:
    type: json
    data: |-
      {
        "done": false,
        "title": "",
        "description": "description modifié"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("le statut est 422", function () {
          expect(res.getStatus()).to.equal(422);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
