info:
  name: Modifier une tache existante Id other user
  type: http
  seq: 21

http:
  method: PATCH
  url: "{{baseURL}}{{apiTasks}}/:id"
  params:
    - name: id
      value: d113eca6-1b84-4b56-b8dd-cb20976f6c2d
      type: path
  body:
    type: json
    data: |-
      {
        "done": false,
        "title": "titre modifié",
        "description": "description modifié"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("le statut est 422", function () {
          expect(res.getStatus()).to.equal(422);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
