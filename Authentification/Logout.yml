info:
  name: Logout
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseURL}}{{apiLogout}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
