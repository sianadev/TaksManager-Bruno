info:
  name: Register Mail vide et mot de passe correct
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseURL}}{{apiRegister}}"
  body:
    type: json
    data: |-
      {
        "email": "",
        "password": "testpwd"
      }
  auth: inherit

runtime:
  scripts:
    - type: before-request
      code: |-
        //bru.setVar("uniqueSuffix", Date.now());

        bru.setVar("randomEmail", bru.interpolate("{{$randomEmail}}"));
        console.log(bru.getVar("registerEmail"));
    - type: tests
      code: |-
        test("le statut est 422", function () {
          expect(res.getStatus()).to.equal(422);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
