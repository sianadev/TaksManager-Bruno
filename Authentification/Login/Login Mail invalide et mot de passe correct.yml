info:
  name: Login Mail invalide et mot de passe correct
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseURL}}{{apiLogin}}"
  body:
    type: json
    data: |-
      {
        "email": "{{mailKO}}",
        "password": "{{passwordOK}}"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("le statut est 401", function () {
          expect(res.getStatus()).to.equal(401);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
