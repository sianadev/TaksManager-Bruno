info:
  name: Login XSS
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseURL}}{{apiLogin}}"
  body:
    type: json
    data: |-
      {
        "email": "{{mailOK}}",
        "password": "<script>alert('test');</script>"
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("le statut est 401", function () {
          expect(res.getStatus()).to.equal(401);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
