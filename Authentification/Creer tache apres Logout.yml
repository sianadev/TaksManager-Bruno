info:
  name: Creer tache apres Logout
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseURL}}{{apiTasks}}"
  body:
    type: json
    data: |-
      //on récupère le token de la req Logout
      //le token est censé être supprimé
      {
        "title": "Titre de test",
        "description": "description de test"
      }
  auth:
    type: bearer
    token: "{{token}}"

runtime:
  scripts:
    - type: tests
      code: |-
        test("le statut est 401", function () {
          expect(res.getStatus()).to.equal(401);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
